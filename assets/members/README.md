# 코어 멤버 사진

이 폴더에 사진 파일을 넣고, `config.js` 의 `members` 항목에 파일명을 적으면
사이트에 표시됩니다.

```js
members: [
    { name: '이지훈', photo: 'leejihoon.jpg' },
    ...
]
```

- 권장: 정사각형(1:1), 400×400px 이상, JPG 또는 WebP
- `photo` 를 비워두면 사진 없이 이름만 표시됩니다
- 파일이 없거나 이름이 틀리면 자동으로 이름 표시로 돌아갑니다 (깨진 이미지가 뜨지 않습니다)
